我们之前讲hystrix时，都是自己在方法上进行配置，但是sentinel则是直接面向配置，而不是编码，所以在web界面定制
熔断规则即可(新版本只有熔断配置，不过熔断和降级本来就是紧密相连的)

区别：
   hystrix只要方法超时异常都会降级，当降级达到一定比例后熔断
   sentinel必须满足指定规则才会熔断(sentinel新版本没有单独的降级配置，直接熔断)，否则不作处理(不像hystrix那样有异常就降级)

sentinel目前新版上异常数熔断虽然可以配置统计时间和最小请求数，但从验证结果来看这个配置是没用的，只要达到了
这个异常数量，那么就会熔断。